import { StyleSheet} from 'react-native';
import {colors} from "./Colors";

export const boxStyle = StyleSheet.create({
    box: {borderWidth: 3, borderColor: colors.blue, backgroundColor: colors.sky}
})